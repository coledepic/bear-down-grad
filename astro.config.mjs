// @ts-check
import { defineConfig } from 'astro/config';

// GitHub Pages project-site config: https://coledepic.github.io/bear-down-grad/
// If you switch to a custom domain, set `site` to it and remove `base`.
export default defineConfig({
  site: 'https://coledepic.github.io',
  base: '/bear-down-grad',
});
